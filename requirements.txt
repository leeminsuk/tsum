fastapi>=0.111.0
uvicorn[standard]>=0.29.0
apscheduler>=3.10.4
requests>=2.31.0
PyYAML>=6.0.1
python-dotenv>=1.0.1
numpy>=1.26.0
supabase>=2.4.0
openai>=1.30.0
tavily-python>=0.3.0

# ── ML 추론 (모델 로드 시 필요) ───────────────────────────────────────────
# 표준 transformer 모델 (CryptoBERT 등):
#   pip install transformers>=4.41.0
#
# PEFT/LoRA 모델 (Llama-3 파인튜닝 결과물):
#   pip install transformers>=4.41.0 peft>=0.11.0 accelerate>=0.30.0
#
# QLoRA 4-bit 추론 (GPU 환경 권장):
#   pip install bitsandbytes>=0.43.0
#
# 서버 배포(Render CPU) 환경에서는 메모리 한계로 8B 모델 로드가 어려울 수 있습니다.
# 이 경우 Hugging Face Hub에 모델을 올리고 별도 GPU 서버에서 서빙하는 방식을 권장합니다.
